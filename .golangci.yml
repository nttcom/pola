version: "2"
linters:
  default: standard
  enable:
    - asasalint
    - asciicheck
    - bidichk
    - containedctx
    - contextcheck
    - copyloopvar
    - depguard
    - dupword
    - durationcheck
    - errchkjson
    - errname
    - errorlint
    - fatcontext
    - gocheckcompilerdirectives
    - gochecknoinits
    - gochecksumtype
    - goconst
    - gocritic
    - gocyclo
    - gosec
    - iface
    - intrange
    - makezero
    - mirror
    - misspell
    - modernize
    - nilerr
    - nilnesserr
    - nolintlint
    - perfsprint
    - reassign
    - revive
    - spancheck
    - testifylint
    - unconvert
    - usestdlibvars
    - wastedassign
  settings:
    depguard:
      rules:
        logging:
          files:
            - '!**/pkg/logger/*.go'
          deny:
            - pkg: log
              desc: use pkg/logger instead
            - pkg: go.uber.org/zap
              desc: use pkg/logger instead
        gobgp:
          files:
            - '!**/internal/gobgp/*.go'
          deny:
            - pkg: github.com/osrg/gobgp/v4
              desc: use internal/gobgp instead
    gochecksumtype:
      default-signifies-exhaustive: false
    goconst:
      ignore-tests: true
      min-occurrences: 3
    gocyclo:
      min-complexity: 15
    iface:
      enable:
        - identical
        - unused
    nolintlint:
      require-explanation: true
      require-specific: true
    revive:
      rules:
        - name: error-naming
        - name: error-strings
        - name: exported
        - name: indent-error-flow
        - name: receiver-naming
        - name: unused-parameter
        - name: unreachable-code
        - name: var-naming
  exclusions:
    generated: lax
    presets:
      - std-error-handling
    rules:
      - path: _test\.go$
        linters:
          - containedctx
          - gosec
      - path: ^tools/coverage-diff/
        linters:
          - gosec
formatters:
  enable:
    - gofumpt
    - goimports
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  uniq-by-line: false
